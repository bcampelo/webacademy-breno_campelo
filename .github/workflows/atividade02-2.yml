name: Segundo workflow

on:
  push:

jobs:

  primeiro-job:
    runs-on: ubuntu-latest
    steps:
        - run: date
        - run: uname -a
        - run: git --version
        - run: cat README.md
        - run: ls

